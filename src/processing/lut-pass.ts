import {RenderingContext} from "../rendering-context";
import {RenderPass} from "./render-pass";

export default class LutPass extends RenderPass {
    constructor(context: RenderingContext) {
        super(context);
    }
}
